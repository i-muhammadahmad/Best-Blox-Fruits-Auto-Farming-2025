import React, { useState, useEffect } from 'react';
import PropTypes from 'prop-types';
import clsx from 'clsx';
import { makeStyles } from '@material-ui/styles';
import {
  Typography,
  Grid,
} from '@material-ui/core';
import { 
  redirectToAuditFormSetupList,
  deleteAuditFormSetup,
  getAuditFormSetupById,
} from 'actions';
import { ViewActionButtons, DeleteAlert } from 'components';
import useRouter from 'utils/useRouter';
import { useSelector, useDispatch } from 'react-redux';

const useStyles = makeStyles(() => ({
  root: {}
}));

const Header = props => {
  const { className, ...rest } = props;

  const classes = useStyles();
  const dispatch = useDispatch();
  const router = useRouter();

  const auditFormSetupState = useSelector(state => state.auditFormSetupState);
  const session = useSelector(state => state.session);

  const [openDeleteModel, setOpenDeleteModel] = useState(false); 

  useEffect(() => {
    if (auditFormSetupState.showViewPage) {
      router.history.push('/audit-form-setup/view');
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [auditFormSetupState.showViewPage]);

  const deleteRecord = async () => {
    await dispatch(deleteAuditFormSetup(auditFormSetupState.auditFormSetupRecord.id, session.current_page_permissions.object_id));
    closeView();
  }

  const showDeleteModal = () => {
    setOpenDeleteModel(true)
  }

  const hideDeleteModel = () => {
  }

  const viewRecord = () => {
    dispatch(getAuditFormSetupById(auditFormSetupState.auditFormSetupRecord.id, 'view'))
  }

  const closeView = () => {
    dispatch(redirectToAuditFormSetupList())
  }

  return (
    <div
      {...rest}
      className={clsx(classes.root, className)}
    >
      <Grid
        alignItems="flex-end"
        container
        justify="space-between"
        spacing={3}
      >
        <Grid item>
          <Typography
            component="h2"
            gutterBottom
            variant="overline"
          >
            AUDIT FORM MANAGEMENT
          </Typography>
          <Typography
            component="h1"
            variant="h3"
          >
            Audit Form Setup
          </Typography>
        </Grid>
        <Grid item>
          <ViewActionButtons  
            btnToShow={['delete', 'view']}
            viewRecord={viewRecord}
            deleteRecord={showDeleteModal}
          />
        </Grid>
      </Grid>
      <DeleteAlert
        title="Audit Form Setup Delete"
        alertText="Are you sure, You want delete this Audit Form Setup?"
        deleteCallback={deleteRecord}
        modalOpen={openDeleteModel}
        handleModalOpen={setOpenDeleteModel}
        onModelClose={hideDeleteModel}
      />
    </div>
  );
};

Header.propTypes = {
  className: PropTypes.string
};

export default Header;
