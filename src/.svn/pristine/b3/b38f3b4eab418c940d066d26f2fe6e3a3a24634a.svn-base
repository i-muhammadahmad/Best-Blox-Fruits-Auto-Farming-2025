import React, { useState, useEffect } from 'react';
import PropTypes from 'prop-types';
import clsx from 'clsx';
import { makeStyles } from '@material-ui/styles';
import { 
  Typography,
  Grid,
} from '@material-ui/core';
import { 
  redirectToActivityLogList,
  deleteActivityLog,
  getActivityLogById
} from 'actions';
import { ViewActionButtons, DeleteAlert } from 'components';
import useRouter from 'utils/useRouter';
import { useSelector, useDispatch } from 'react-redux';

const useStyles = makeStyles(() => ({
  root: {}
}));

const Header = props => {
  const { className, ...rest } = props;

  const classes = useStyles();
  const dispatch = useDispatch();
  const router = useRouter();
  const activityLogState = useSelector(state => state.activityLogState);
  const session = useSelector(state => state.session);

  const [openDeleteModel, setOpenDeleteModel] = useState(false);

  useEffect(() => {
    if (activityLogState.showViewPage) {
      router.history.push('/activity-log/view');
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [activityLogState.showViewPage]);

  const deleteRecord = async () => {
    await dispatch(deleteActivityLog(activityLogState.activityLogRecord.id, session.current_page_permissions.object_id));
    closeView();
  }

  const showDeleteModal = () => {
    setOpenDeleteModel(true)
  }

  const hideDeleteModel = () => {
  }

  const viewRecord = () => {
    dispatch(getActivityLogById(activityLogState.activityLogRecord.id, 'view'))
  }

  const closeView = () => {
    dispatch(redirectToActivityLogList())
  }

  return (
    <div
      {...rest}
      className={clsx(classes.root, className)}
    >
      <Grid
        alignItems="flex-end"
        container
        justify="space-between"
        spacing={3}
      >
        <Grid item>
          <Typography
            component="h2"
            gutterBottom
            variant="overline"
          >
            Activity Management
          </Typography>
          <Typography
            component="h1"
            variant="h3"
          >
            Activity Log
          </Typography>
        </Grid>
        <Grid item>
          <ViewActionButtons  
            viewRecord={viewRecord}
            deleteRecord={showDeleteModal}
            btnToShow={['delete', 'view']}
          />
        </Grid>
      </Grid> 
      <DeleteAlert
        title="Activity Log Delete"
        alertText="Are you sure, You want delete this Activity Log?"
        deleteCallback={deleteRecord}
        modalOpen={openDeleteModel}
        handleModalOpen={setOpenDeleteModel}
        onModelClose={hideDeleteModel}
      />   
    </div>
  );
};

Header.propTypes = {
  className: PropTypes.string
};

export default Header;
